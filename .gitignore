# ไฟล์และโฟลเดอร์ผลลัพธ์จากการรันโมเดล (มีขนาดใหญ่)
captures/
case_*/
reports/

# ไฟล์วิดีโอที่ใช้เทส
*.mp4
*.avi

# ไฟล์โมเดลขนาดใหญ่ (ถ้ามี)
*.pt
*.weights

# โฟลเดอร์อื่นๆ ที่ไม่จำเป็นต้องเอาขึ้นเว็บ
__pycache__/
.vscode/
uploads/